import 'package:flutter/material.dart';

class InterviewBlock extends StatelessWidget {
  const InterviewBlock({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
